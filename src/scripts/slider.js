// 'use strict'

// ;(function ($) {
//   $(document).ready(function () {
//     $(document).ready(function () {
//       let currentFeatureIndex = 0
//       const features = $('.feature')

//       $('.features__navigation--forward').click(function (e) {
//         e.preventDefault()
//         // Hide the current feature by sliding it to the left
//         $(features[currentFeatureIndex]).css('left', '-100%')

//         // Increment the feature index
//         currentFeatureIndex = (currentFeatureIndex + 1) % features.length

//         // Show the next feature by sliding it in from the right
//         $(features[currentFeatureIndex]).css('left', '0')
//       })

//       $('.features__navigation--back').click(function (e) {
//         e.preventDefault()
//         // Hide the current feature by sliding it to the right
//         $(features[currentFeatureIndex]).css('left', '100%')

//         // Decrement the feature index
//         currentFeatureIndex =
//           (currentFeatureIndex - 1 + features.length) % features.length

//         // Show the previous feature by sliding it in from the left
//         $(features[currentFeatureIndex]).css('left', '0')
//       })
//     })
//   })
// })(jQuery)
